(function ($, angular) {
'use strict';
/**
 * @ngdoc service
 * @name bestline.defaultFactory
 * @description
 * # defaultFactory
 * Factory in the bestline.
 */
angular.module('bestline')
.factory('defaultFactory', function () {
  
  var defaultFactory = {};//object returned from factory
  
  return defaultFactory;
});
}(window.jQuery || window.$, window.angular));